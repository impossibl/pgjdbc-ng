language: java

before_script:
  - psql -U postgres -c "create user pgjdbc with password 'test';"
  - psql -c 'create database test owner pgjdbc;' -U postgres

matrix:
  include:
    - jdk: openjdk7
      addons:
        postgresql: "9.4"